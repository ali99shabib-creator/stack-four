# Stack Four

Classic wooden Stack Four game.

## Automatic APK build
Every push to `main` starts the GitHub Actions workflow.
You can also run it manually from **Actions → Build Android APK → Run workflow**.

After the workflow finishes, download the `StackFour-APK` artifact.
